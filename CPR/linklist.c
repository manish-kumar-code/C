#include<stdio.h>
#include<stdlib.h>
struct node
{
    int data;
    struct node *next;
}
struct node *head,*temp;
struct node *getnode()
{
    struct node *p;
    p=(struct node*) malloc(size of(struct node));
    return p;
}
void insert(int n)
{
    struct node*newnode,*node;
    int data i;
    head=getnode();
    printf("enter data of enter:");
    scanf("%d",&data);
    head 
}